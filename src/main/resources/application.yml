springboot:
  jwt:
    secret: flature!@#

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:tcp://localhost/~/test  # H2 ???? ?????? URL
    username: sa            # ?? ??? ??
    password:               # ???? (???? ?? ??)
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

h2:
  console:
    enabled: true
    path: /h2-console
